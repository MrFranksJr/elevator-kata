import {ElevatorCall} from "./elevator-call";

export class ElevatorController {
    public handleCalls(call: ElevatorCall[]) {
        //TODO
    }

    public getCurrentElevatorFloor(): number {
        //TODO
        return -1;
    }
}